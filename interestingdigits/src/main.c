#include <stdio.h>

#include "log.h"

int main(int argc, char *argv[])
{
	int base;

    if (argc != 2) {
        log_error("Usage: interesing-digits [BASE]");
        return -1;
    } else if (

    return 0;
}
